<%= form_for(@user) do |f| %>
<%= f.label :name %>
<%= f.text_field :name, placeholder: @user.name %>
<%= f.label :email %>
<%= f.text_field :email, placeholder: @user.email %>
<%= f.submit %>
<% end %>